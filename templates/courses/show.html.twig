{% extends 'base.html.twig' %}

{% block title %}Courses{% endblock %}

{% block body %}
    <h1 class="text-center" id="h4mat">Courses</h1>

    <table class=" table table-striped container" id="con">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col"> Image</th>
            <th scope="col">Date de création</th>
            <th scope="col">Description</th>
    </thead>
        <tbody>
            <tr> 
                <th scope="row">{{ course.id }}</th>
                <td>{{ course.name }}</td>
                <td>{{ course.image }}</td>
                <td>{{ course.dateCreate ? course.dateCreate|date('Y-m-d') : '' }}</td>
                <td>{{ course.description }}</td>
            </tr>
        </tbody>
    </table>
    <div class="sup">
    {{ include('courses/_delete_form.html.twig') }}
    <div class ="sup">
    <button class="btn btn-lg btn-primary mr-5" id="rightbtn"  type="submit" onclick="location.href='{{ path('courses_edit', {'id': course.id}) }}';">
            Modifier
    </button>
    <button class="btn btn-lg btn-primary " id="rightbtn"  type="submit" onclick="location.href='{{ path('courses') }}';">
            Retour 
    </button>

    </div>

    
{% endblock %}
